We created surveys for control and treatment groups 
the process of collecting the data is going on

open terminal and run
pip install -r requirements.txt
cd src
export QUALTRICS_API_TOKEN="your Api key here"
export QUALTRICS_DATA_CENTER="yul1"
python import_os.py